# 开发记录

## 2026-08-17 投票阶段同步思考状态（TASK-062）

### 本轮目标

投票采用并行生成时，让所有具备投票资格且尚未完成的玩家同时进入思考视觉状态，不再只显示状态机当前行动者；保持秘密目标、重投资格和统一揭票规则不变。

### 完成内容

- `GameScreen` 改为根据 `actionType`、存活状态、重投候选集合和 `voteProgress.completedPlayerIds` 统一派生座位状态，不依赖单个 `currentActorId` 表示投票思考。
- 普通投票中，所有未完成玩家显示“思考中”；完成者恢复待机并显示“已投票”。重投候选保持被怀疑状态，只有有资格的非候选进入思考。
- 新增初始投票、部分完成和重投三类组件断言；更新前端规格，并以 DEC-087 修正 DEC-074 的逐个思考视觉。

### 验证结果与边界

- `GameScreen.test.tsx` 定向 Vitest 23/23 通过，Web TypeScript `--noEmit` 通过，`git diff --check` 通过。
- 内置浏览器使用强制 `AGENT_PROVIDER=fake` 的本地对局验证：投票进度 `0/4` 时人类、DeepSeek、豆包、千问四个座位均显示“思考中”；人类提交后正常统一揭票并淘汰。
- 本轮只改变公开进度的视觉派生，不公开未揭晓目标，不改变服务端并行预取、顺序提交、持久化或真实模型策略；未读取 Key，未产生真实模型调用或费用。

本次属于既有投票流程的体验优化，不构成产品版本发布，不分配版本号。

## 2026-08-17 收口当前文档一致性（TASK-061）

### 本轮目标

根据当前工作区代码和已有验收报告，修正文档对真实模型验收、异步 AI 总结、媒体接入和素材路径的过期描述；保持“已有基础设施”与“用户可用闭环”之间的边界，不改业务代码。

### 完成内容

- README、CLAUDE 与规格索引同步当前能力：重复泄词强退、系统终止、背景与 BGM 已实现；历史列表和 Markdown 导出仍未实现。
- 明确异步 AI 总结已经具备服务端任务、`review_summaries` 持久化和查询/重新生成 API，但当前 `ReviewScreen` 未请求或展示 `ReviewSummary`，因此不能标记为完整用户功能。
- TASK-057 同步两份脱敏报告的实际证据：3 个模型共 6 次策略调用通过，纯 shared 状态机整局到达 `finished`；HTTP/SQLite 整局仍受本机 `better-sqlite3` 环境限制，任务继续保持进行中。
- `ASSETS.md` 改以 `apps/web/src/assets/` 为当前事实源，清理已删除根目录 `素材/` 的失效链接，修正背景和 BGM “尚未接入”以及 BGM 默认状态的旧描述。

### 验证结果与边界

- 使用文本检索复核异步复盘、重复泄词、背景、BGM、素材根路径与 TASK-057 状态。
- `git diff --check` 通过。本轮仅修改 Markdown，不改变运行时代码，因此不重复执行应用测试。
- 未执行真实模型调用，未读取密钥，未提交或推送。

本次属于文档一致性维护，不构成产品版本发布，不分配版本号。

## 2026-08-17 统一 Agent 校验与自动恢复（TASK-058、TASK-059）

### 本轮目标

先同步需求与运行时契约，再统一 Agent 错误分类、自动恢复和最终安全提示。模型输出或持久化出现可恢复问题时尽量自动处理；只有恢复耗尽才通知玩家，重复泄词按公开规则强退。

### 完成内容

- `TokendanceAgentPolicy` 使用 strict 描述/投票 Schema，字段缺失、类型、信念或目标错误先做一次结构修复；超时、网络、429、5xx 与空/坏响应按预算重试，401/403、模型不存在或未配置等永久错误不盲目重试，最终只抛脱敏 `AgentSystemError`。
- `GameService` 在提交前校验 AI 公开发言：长度/句数错误与首次原词泄露秘密作废并重生成；第二次泄词发布不含原文的 `player_rule_violated`，强退违规玩家并立即判断胜负。模型返回后重新检查 revision、状态、行动者和动作类型，过期结果直接丢弃。
- 已验证模型输出提交失败时只重试同一数据库提交，不再次调用模型；未知程序异常保持抛出，继续使用既有重启恢复机制。
- shared 增加内部违规强退命令、公开安全事件和 `player_rule_violation` 终局原因；Web 订阅违规/系统终止事件，展示安全分镜和可开始新局的终止提示，不渲染失败原文或私有词牌。
- README、Agent/API/领域规格、测试规范、任务台账、checklist 与命令记录同步到当前实现；TASK-057 的付费真实模型分层验收仍保持进行中。

### 验证结果与边界

- Vitest 156/156：shared 47、server 64、web 45；覆盖严格结构修复、可重试/永久错误、内容重生成、首次/重复泄词、过期结果、提交复用、异常终止、SSE 监听与 DOM 隔离。
- shared/server/web typecheck、ESLint、shared/server/Web 生产构建全部通过；`git diff --check` 通过。
- Playwright E2E 10/10：normal 4、spectator 4、tie 2，均覆盖 Desktop Chrome 与 Pixel 5，服务端强制 `AGENT_PROVIDER=fake`、Key/URL 为空。
- 内置浏览器实测 `创建对局 -> 确认词牌 -> 开始游戏 -> 第 1 轮人类描述`，页面非空、无框架错误遮罩、控制台零 warning/error，操作区和公开时间线正常。
- 工作区 `node_modules` 的 Windows junction 损坏，本轮在 Node 22、同版本依赖和当前源码的 hoisted 隔离副本执行门禁；验证配置不进入仓库。未执行 `test:live`，未读取 Key，未产生真实模型调用或费用。

本轮未提交或推送，不构成产品版本发布，不分配版本号。

## 2026-08-17 收口人类回合操作体验（TASK-060）

### 本轮目标

优化人类回合的操作可达性与提交前反馈：不改变角色状态提示或时间线阅读层级，只让描述、辩解和投票操作在需要时更容易进入视口，并说明禁用提交的原因。

### 完成内容

- `GameScreen` 为人类描述、辩解和投票回合建立一次性操作标识；操作区首次进入且位于视口外时才平滑定位到可见范围。同一行动不会重复定位，因此不影响用户手动回看时间线。
- 描述和辩解输入增加关联的可访问提示：空输入明确说明“至少输入 2 个字后可提交；最多 40 字，不能直接说出原词”；长度、句数或泄词校验失败时展示具体原因。
- `human-action` 元信息可换行，窄屏下计数和提示不会相互挤压；补充组件测试，覆盖初始提交条件和离屏操作区定位。
- 更新 `frontend-ux.md`、任务台账和 checklist，使体验规格、实施状态与验收记录一致。

### 验证结果与边界

- `git diff --check` 通过。
- 当前本机依赖链接不完整，Vitest 缺少 `@vitest/utils`，服务端 `better-sqlite3` 在 Node 24 下缺少可用原生构建环境，无法重新启动完整本地对局。
- 负责人已确认本次桌面与移动验证通过，按确认结果完成 TASK-060 收口。

本次属于既有对局流程的体验优化，不构成产品版本发布，不分配版本号。

## 2026-08-17 按模型家族关闭推理链（TASK-056）

### 本轮目标

修复负责人反馈的"千问思考时间过久、前端解析不出且多次调用、无法放弃对局"。要求为 deepseek、豆包(seed)、千问(qwen) 三个游戏模型分别按厂商关闭推理链、让输出尽可能快，其他模型暂不受影响，随后推送。

### 根因（本机真实中转站实测，用户授权）

- 千问 `qwen3.7-plus` 推理开启单回合约 48s，超过客户端默认超时 20s → `AbortError` → 系统重试 → "多次调用"，且回合始终未提交，前端一直停留在"正在思考"。放弃按钮虽在数据库层受 revision 乐观锁保护，但被这波重试风暴的延迟拖住，表现为"无法放弃"。
- 解析并非瓶颈：抓取真实响应，`extractJson` 结果 `ok:true`，content 是合法 JSON；`reasoning_content` 与 content 分离。

### 完成内容

- **新增 `agents/model-reasoning.ts`**：`reasoningDisableBodyFor(modelId)` 按模型家族（小写、去空白、前缀匹配）返回关推理参数——`qwen*`→`{enable_thinking:false}`；`seed*`/`doubao*`→`{thinking:{type:'disabled'}}`；`deepseek*`→`{thinking:{type:'disabled'}}`；其他→`{}`（行为不变）。仅模型请求体字段，绝不含 Key/URL。
- **`tokendance-client.ts`**：`chatCompletion` 支持每次调用透传 `extraBody`，合并顺序 `{...defaultBody, ...extraBody, model, messages}`（家族专用参数覆盖通用兜底）。
- **`tokendance-agent-policy.ts`**：按解析到的 `modelId` 计算 `reasoningDisableBodyFor` 并随每次调用下发。
- **`server.ts`**：默认 `TOKENDANCE_TIMEOUT_MS` 20000→60000，作为兜底避免推理延迟触发重试风暴。
- **测试**：新增 `model-reasoning.test.ts`（家族映射、大小写/空白、未知模型不附加）；扩展 `tokendance-agent-policy.test.ts` 的 `ScriptedClient` 捕获 `extraBody`，断言各家族下发正确参数、其他模型为空。
- **文档**：更新 `.env.example`（默认超时说明、家族关推理已自动、`EXTRA_BODY` 仅通用兜底）、`docs/spec/agent-runtime.md §11`、`TASKS.md` 登记 TASK-056。

### 实测效果（真实付费冒烟，脱敏）

- qwen `enable_thinking:false`：约 48s → 7s。
- seed `thinking.type=disabled`：约 168s → 7.4s，content 仍合法 JSON、`extractJson ok:true`。
- deepseek `thinking.type=disabled`：约 12s → 1.5s，无 `reasoning_content`（注：deepseek 忽略 `enable_thinking`）。
- 三个参数即便被某厂商忽略也返回 HTTP 200，安全。

### 验证结果与边界

- `pnpm typecheck`、`pnpm lint` 通过；`pnpm test` 全绿（shared 46 / server 55 / web 33）；`pnpm build` 通过。web 首跑偶发 1 项失败（`App.test.tsx` 既有 jsdom 计时抖动，与纯服务端改动无关），复跑 33/33。
- 硬边界保持：Base URL / API Key / 请求头 / 完整模型响应绝不进入浏览器、数据库、日志、仓库或复盘；默认 `dev/test/test:e2e` 永不联网。诊断用临时脚本 `tests/live/diag-qwen.mjs` 已删除，非提交件。
- 本轮为负责人明确授权的一次性 push。

本次为里程碑之外的运行时优化，不构成产品版本发布，不分配版本号。

## 2026-08-17 收口视觉与媒体（TASK-045）

### 本轮目标

收口 TASK-045 视觉与媒体：确认 BGM 开关与纸面/审讯室背景切换已接入，补齐缺失的组件测试，规范化音频素材路径，并完成桌面/移动验证与文档。

### 完成内容

- **音频素材规范化**：把 `bgm.wav` 从仓库根中文目录 `素材/` 复制到 `apps/web/src/assets/audio/game-bgm.wav`，`character-assets.ts` 改为本地英文路径导入，去掉 `../../../素材/bgm.wav?url` 跨包中文路径，与角色/场景素材的归一化约定一致。
- **组件测试补齐**：新增 `experience-settings.test.tsx`（8 项）覆盖 `ExperienceControls` 展示与回调、`useExperienceSettings` 默认关闭、localStorage 恢复与持久化、纸面/审讯室背景切换与场景底图 URL，以及"BGM 音源不再指向中文素材目录"的负向断言。
- **确认既有实现**：BGM 开关（默认关、循环、音量 0.24、自动播放拦截提示、StrictMode 卸载安全）与背景切换（`shell--interrogation` + `--scene-background` 经 `::before` 渲染、选择持久化）此前已实现并接入 `App`。
- **文档同步**：更新 `docs/notes/ASSETS.md`（背景与 BGM 均已接入并规范化、10.5MB WAV 待发布前压缩）、`TASKS.md` TASK-045 置为已完成。

### 验证结果与边界

- `pnpm typecheck`、`pnpm lint` 通过；`pnpm test` 128 项全绿（shared 46 / server 49 / web 33，含新增 8）；`pnpm build` 通过并把 `game-bgm-*.wav` 打入 `dist/assets`。
- 桌面与 375×812 移动端用强制假模型服务端 + Web 预览实测：审讯室背景经 `::before` 渲染于 shell 之后，设置控件（对局/模型档案导航、背景音乐开关、纸面/审讯室单选）布局与素材加载正常。
- `game-bgm.wav` 为 10.5MB 未压缩 WAV，构建提示体积偏大；本机无 ffmpeg，正式发布前应转码压缩。
- 本轮未提交或推送（沿用负责人夜间指令：单次授权 push 已完成，后续开发-测试循环不再 push）。

本次完成里程碑之外的视觉媒体收口，不构成产品版本发布，不分配版本号。

## 2026-08-17 优化开始/推进卡顿与修复 E2E 隔离漏洞

### 本轮目标

修复负责人反馈的"点击开始以后不会自动跳转，而且速度特别慢"，并在此过程中修复一个更严重的隐患：E2E 会读取本机 `.env` 的真实 Tokendance Key 并联网烧付费额度。

### 完成内容

- **运行时后台推进（backgroundAdvance）**：`GameService` 新增依赖注入开关 `backgroundAdvance`。运行时置 `true`，`startGame` 与人类操作持久化本次转移后立即返回，AI 回合经 `settleAdvance` 异步推进、经 SSE 逐帧下发；界面立即跳入对局并显示"某 AI 正在思考"，不再卡在"正在开始…"。测试默认 `false`（同步 `await`），断言可确定读到已推进状态。后台推进失败仅脱敏记 `error.name`，绝不外泄 Key/URL。
- **修复 E2E 联网隔离漏洞**：`main.ts` 的 `loadDotEnv()` 无条件加载本机 `.env`；本机配了 `AGENT_PROVIDER=tokendance`+真实 Key 时，E2E 服务端会走真实策略、每次跑都联网并消耗付费额度，且真实模型往返使对局无法在测试时限内完成。在 `playwright.config.ts` 的服务端 `webServer.env` 预置 `AGENT_PROVIDER=fake`、空 `TOKENDANCE_*`；因 `loadDotEnv` 不覆盖"已存在的环境变量"，服务端一律回退 `FakeAgentPolicy`，E2E 绝不联网、绝不读 Key。
- **E2E 轮询兜底**：`helpers.ts` 的 `playUntilTerminalOrSpectator` 改用挂钟截止时间（60s）兜底，避免把后台推进期间"当前是 AI 行动者"的自旋等待计入固定步数预算而误判超时。
- 清理 `game-service.ts` 中排障期临时加入的 `[DBG]` 调试日志。
- 文档同步：`agent-runtime.md` 新增 §2.1「推进模式：后台推进与测试同步」；`TASKS.md` 登记 TASK-055。

### 验证结果与边界

- `pnpm typecheck`、`pnpm lint` 通过；`pnpm test` 120 项全绿（shared 46 / server 49 / web 25）；`pnpm test:e2e` 10 项全绿（normal 4 / spectator 4 / tie 2）。
- 用本地探针在强制假模型下验证对局可在后台推进中完整跑到终局。
- 真实模型（tokendance）下 doubao 等 model 往返较慢属中转站/模型侧特性；后台推进 + 超时重试已让其优雅降级，负责人可按需换更快 model 或用 `TOKENDANCE_EXTRA_BODY` 关推理链提速。
- `.env` 始终被 gitignore；本轮未提交任何 Key/URL。

本次属于既有功能的性能与隔离修复，不构成产品版本发布，不分配版本号。

## 2026-08-16 扩充首版 30 组词库

### 本轮目标

将首个里程碑使用的 4 组词库子集扩充为首版正式内容量，保持 JSON 事实源、固定阵营和难度筛选契约不变。

### 完成内容

- `data/word-pairs.json` 扩充到 30 组并全部启用：简单 15 组、困难 15 组。
- 简单词覆盖饮品、动物、主食、水果、文具、数码、交通、生活用品、运动、职业和场所，优先提供直观可描述特征。
- 困难词覆盖自然、工具、自然现象、抽象概念、信息、行为、声音、时间、空间、物件、文学、交流、推理、语言行为和状态，强调相近关系或抽象边界。
- 保留原有 4 个稳定 ID 和词组，新增词组继续使用英文语义 ID；固定平民词/卧底词映射不随机交换。
- `word-pairs.test.ts` 增加对真实 JSON 事实源的断言：总数 30、每档 15、全部启用、组合唯一，并继续复用集合 Zod 校验。
- TASK-044 登记并完成；README 与规格不再把完整 30 组词库写成未实现。

### 验证结果与边界

- 文件级核对确认 30 个 ID、15 个 easy、15 个 hard。
- 当前命令执行环境不可用，无法在本轮重新运行 `pnpm test/typecheck/lint/build`；已新增自动化断言，待环境恢复后补跑完整门禁。
- 词组已完成首轮人工筛选；真实多模型接入后仍应通过实际对局统计复核难度和阵营公平性，必要时使用 `enabled=false` 停用问题词组。

本次完成首版内容范围，但不构成产品版本发布，不分配版本号。

## 2026-08-16 对齐当前代码与项目文档

### 本轮目标

系统对照当前 shared、server、web、数据库 Schema、测试和素材代码，修正文档中已经落后、超前或把规划误写成现状的内容。

### 完成内容

- README 补齐核心设计、AI 工具与验收、信息隔离、当前实现、已知边界和迭代记录入口。
- `docs/spec/README.md` 增加当前实现边界，明确首个里程碑已完成及后续未实现能力。
- 更新前端规格：五个角色五状态素材已接入；背景仅完成复制/映射、页面未消费；音乐未接入；当前无模型档案和历史列表。
- 更新 Agent 规格：当前仅有 `normal` 与 `tie-then-eliminate` 两个 `FakeAgentScenario`；真实模型、格式修复、系统重试和异常终止尚未实现。
- 更新 API/领域规格：`system_terminated`、`MODEL_ACTION_FAILED`、`stream_error` 等后续契约标为预留；当前终局字段边界按代码说明。
- 更新持久化规格：对齐现有 `agent_actions` 和 `agent_role_models` 物理列，明确 `model_attempts` 和异步复盘表未实现。
- 更新测试规格：补入当前 104 项 Vitest 与 10 项 Playwright 的文件级测试清单，删除不存在的假模型场景声明。
- 更新素材索引：对齐角色 PNG 的实际接入路径、背景的代码映射和音乐未接入状态。

### 验证边界

- 历史切片记录保留当时状态，不回写旧记录中的“下一切片”或历史测试数量；它们是时间点证据，不作为当前事实源。
- 本轮只更新文档，不改变产品代码、数据库或测试行为，也未执行 Git 提交或推送。

本次属于文档一致性维护，不分配产品版本号。

## 2026-08-16 完成切片 7 自动化验收与首个里程碑收口

### 本轮目标

用真实 Web/Server、可控假模型和独立 SQLite 完成首个里程碑端到端验证，汇总全通道信息隔离证据，并让命令、README、任务和验收文档反映实际完成状态。

### 完成内容

- Playwright 配置自动启动 Fastify 与 Vite；服务端增加仅供假模型/E2E 使用的 `FAKE_RANDOM_SEQUENCE`，可稳定复现人类/AI 卧底与平票场景。
- `pnpm test:e2e` 串行运行 normal、spectator、tie 三套独立数据库场景，桌面 Chrome 和 Pixel 5 共 10 项：正常完整终局与刷新恢复、进行中二次确认放弃、淘汰后继续观战/放弃、平票辩解/重投/淘汰。
- E2E 浏览器操作覆盖创建、准备、开始、确认弹窗、观战选择、终局揭晓、事实入口和新对局；重复的人类行动通过页面同源 REST 驱动，避免 SSE 重渲染导致测试点击竞态，结果仍在真实浏览器 DOM 验证。
- 全通道负向矩阵补齐普通投票 Agent 输入、REST 原始响应、SSE 投票进度/补发帧、浏览器 DOM 和 Fastify 日志边界；正常终局前不存在完整揭晓和胜者字段。
- README 增加 E2E 安装/运行说明与已知边界；任务台账、里程碑 checklist 和验收清单全部收口。

### 验证结果

- `pnpm test`：104 项通过（shared 43、server 40、web 21）。
- `pnpm test:e2e`：10 项通过（normal 4、spectator 4、tie 2），覆盖桌面与移动项目。
- `pnpm build`、`pnpm typecheck`、`pnpm lint` 和 `git diff --check` 全部通过。
- 默认测试、构建和 E2E 均未读取密钥、访问真实模型或产生付费调用；`test:live` 保持显式失败占位。
- 正常流程、平票、恢复、放弃、观战和终局均有自动化证据；浏览器控制台、网络业务请求和服务端日志无意外错误或私有字段。

### 已知边界

- 系统异常终止、模型错误重试、真实模型、异步 AI 总结、历史列表/Markdown 导出、重复泄词强退、完整 30 组词库和最终素材优化延后。
- E2E 首次运行需执行 `pnpm exec playwright install chromium`；浏览器运行时文件不进入仓库。
- 当前仓库尚无首个 Git `HEAD`，只读子代理无法以工作树启动；不影响测试与构建，但提交前需按负责人指令创建首个提交。

七个切片均已完成，尚未执行 Git 提交或推送；等待负责人明确指令。

## 2026-08-16 完成切片 6 放弃、淘汰观战与终局展示

### 本轮目标

补齐基础玩法闭环中的主动退出、人类淘汰后选择和终局事实展示，同时继续保证正常终局前的信息隔离与刷新恢复。

### 完成内容

- 共享状态机新增准备、进行中和等待观战状态的 `AbandonGame`，进入 `abandoned/ended` 且不产生 `winnerCamp`；人类非终局淘汰进入 `awaiting_spectator`，`ContinueSpectating` 恢复下一轮并由 AI 自动推进。
- 正常终局在胜负、快照和全部事实提交后产生唯一公开 `terminal_reveal_ready`；`HumanGameView` 仅在 `finished` 返回按座位排列的 `reveal` 与 `factReview`，放弃、进行中和观战状态禁止这些字段。
- 服务端从已持久化 `agent_actions` 按回合和修订号重建事实复盘，终局首次响应、幂等重放和刷新视图一致；公开时间线继续只读取公开事件。
- Web 接通准备/进行中二次确认放弃、等待观战的继续/放弃选择、观战提示、放弃终局、新对局入口、按座位自动揭晓和事实复盘展开。
- 终局动画只消费服务端事实；浏览器保存最近对局 ID，在活动局为空时恢复最近正常终局或放弃结果；`prefers-reduced-motion` 下直接展示全部身份。

### 验证结果

- 全仓 103 项默认测试通过：shared 43、server 39、web 21。
- `pnpm typecheck`、`pnpm lint`、`pnpm build` 与 `git diff --check` 全部通过。
- 服务端流程测试覆盖准备/进行中/等待观战放弃、观战继续、正常终局揭晓、事实顺序、刷新不重复与唯一 `terminal_reveal_ready`。
- 负向测试证明准备、进行中、等待观战和放弃视图不存在 `reveal`、`factReview`、其他词牌、阵营、AI 信念或理由；放弃视图不存在 `winnerCamp`。
- 真实浏览器在桌面和 375×812 移动视口完成二次确认放弃、正常终局、刷新恢复、四张身份牌、事实复盘和开始新对局；移动终局牌为两列，无横向溢出。
- 减少动态效果由组件测试证明不创建揭晓定时器并立即开放事实入口；浏览器控制台和 Server 日志无错误。进行中 SSE 的 `ERR_ABORTED` 仅发生在状态切换/刷新时主动关闭连接，符合预期。

### 已知边界

- 异步 AI 总结、历史复盘列表/导出、真实模型、异常终止与重复泄词强退不在首个里程碑当前切片范围。
- 终局事实复盘当前提供公开时间线与 AI 私有行动/投票理由的确定性入口；更丰富的逐行动信念展开随后续复盘界面完善。
- 下一步进入切片 7，完成 E2E、全通道信息隔离和文档收口。

本次形成可识别的开发切片，但尚未形成产品版本，不分配版本号。

## 2026-08-16 索引无水印对局背景

- 新增并检查 `素材/背景图.png`，尺寸为 2600 × 1460，约 16:9，已去除水印。
- 背景的冷灰蓝、手绘墨线和纸张颗粒与角色素材一致，中央及下方留白可叠加角色、气泡和状态 UI，可直接用于原型。
- 保留原始中文文件名；前端接入时复制并规范化为 `scenes/interrogation-room.webp`，不执行破坏性重命名。
- 移动端先使用固定焦点响应式裁切，实际验证不足时再补竖版。

本次属于素材索引更新，不分配产品版本号。

## 2026-08-16 索引背景音乐

- 新增并索引 `素材/bgm.wav`，用于对局背景音乐。
- 原型可直接接入；浏览器在用户首次主动操作后播放，并提供静音或音量控制。
- 原始 WAV 作为母带保留，发布前生成 OGG/MP3 压缩副本，并核验来源授权、音频参数、响度和循环接缝。
- 当前环境无法读取详细音频元数据，因此时长、采样率、声道和码率标为接入前核验项。

本次属于素材索引更新，不分配产品版本号。

## 2026-08-16 索引人类剪影并划分剩余素材来源

- 新增男、女人类剪影共 10 张，分别覆盖待机、思考、发言、被怀疑和被淘汰状态；当前角色素材总数为 25 张。
- 人类剪影可直接进入与 AI 相同的角色状态容器，接入时生成 `human-male` 与 `human-female` 的规范化 WebP 副本。
- 剩余素材按制作方式划分：场景背景、少量装饰物和可选纸张纹理适合 AI 生图；通用功能图标、开源中文字体和可选音效适合采用许可清晰的现成资源；词牌、投票、印章、气泡、分镜、进度、动画和全部交互 UI 由开发直接使用 HTML/CSS/SVG 实现。
- 不使用 AI 生成包含文字或精确状态的 UI，也不寻找现成角色和整套游戏 UI，避免文字失真、响应式受限、画风割裂与授权风险。
- 更新 `docs/notes/ASSETS.md` 和前端规格；剩余 AI 生图项属于后续视觉润色，不阻塞当前功能切片。

本次属于素材索引与制作规划更新，不分配产品版本号。

## 2026-08-16 完成切片 5 事务、幂等与恢复

### 本轮目标

强化切片 1–4 已有持久化链路，使事件、私有动作、公开帧、快照和命令结果具备可证明的原子性；服务重启、重复恢复和 SSE 重连不得丢失、重复或倒退已确认动作。

### 完成内容

- `saveStarted` 收敛到通用 `commitTransition`；所有非创建转换统一按旧修订号条件提交事件、`agent_actions`、`public_stream_entries`、快照和 `processed_commands`。
- `findSnapshot` 对快照的 `eventSeq/streamSeq` 与对应表高水位做一致性校验，损坏时抛出 `DATA_INCONSISTENT`，不猜测修复或继续自动推进。
- `GameService` 增加单局进程内自动推进锁、活动局启动恢复和幂等 `resumeGame`；AI 动作继续使用 `gameId/revision/actor/actionType` 稳定 ID 和数据库唯一约束。
- Server `onReady` 恢复轮到 AI 的活动局；准备阶段和人类回合保持等待；SSE 建连前也触发同一幂等恢复入口。
- SSE 支持 `Last-Event-ID` 和 `after`，查询参数优先；非法/负数游标回退为 0；补发过程中维护递增游标。Web 使用当前 `eventCursor` 建连，并只接受游标严格前进的新视图。
- 文档重整为 tasks、notes、acceptance、history 四类目录，TASK-000～032 和切片 checklist 持久化，后续任务与检查点要求实时更新。

### 验证结果

- 全仓 91 项测试通过：shared 39、server 34、web 18。
- 新增事务故障注入 5 项：在事件、私有动作、公开帧、快照更新、processed command 任一步失败时，各表和快照整体回滚，移除故障后同一转换可以安全重试。
- 新增重启恢复 4 项：准备等待、AI 描述/投票中断恢复、稳定 actionId 唯一、二次重启计数不变、高水位损坏拒绝。
- 新增 SSE 3 项和 Web 去重 1 项：`events?after`、真实流式 `Last-Event-ID` 补发、游标优先级/回退、SSE URL 携带 cursor、旧/同 cursor 不覆盖。
- `pnpm typecheck`、`pnpm lint`、`pnpm build` 与 `git diff --check` 全部通过。
- 浏览器刷新前后公开时间线均为 19 个分镜，无重复；页面控制台、网络请求和 Server 日志均无错误。
- 新增测试继续断言安全帧中不存在阵营、其他词牌、AI 信念与投票理由。

### 已知边界

- `awaiting_spectator` 节点和人类淘汰后的恢复尚未实现，随切片 6 一并验收。
- 当前 SSE 采用持久帧补发加短间隔 flush；不依赖内存广播，因此正确性优先于即时推送延迟。
- 本切片不实现人类主动放弃、终局身份揭晓或事实复盘界面。
- 下一切片实现放弃、淘汰观战与终局展示。

本次形成可识别的开发切片，但尚未形成产品版本，不分配版本号。

## 2026-08-16 重整文档信息架构并固化任务台账

### 本轮目标

把实施任务和检查点从单次会话工具中持久化，并按负责人要求将任务/checklist、注意事项、验收标准、历史交流分别归类，建立人类与 Claude 共用的统一阅读入口。

### 完成内容

- 新增 `docs/README.md`，明确阅读顺序、事实源优先级和实时文档更新规则。
- 建立 `docs/tasks/`、`docs/notes/`、`docs/acceptance/`、`docs/history/` 四类目录及索引；工程契约继续独立保留在 `docs/spec/`。
- 移动协作、素材、决策、需求、测试、里程碑、命令、开发记录和聊天归档文档，未复制第二份事实源。
- 新增 `docs/tasks/TASKS.md`，完整登记 TASK-000～TASK-032，并同步 TASK-033～035 文档治理工作；TASK-000 按负责人确认定义为项目启动基线。
- 新增 `docs/tasks/milestone-1-checklist.md`，固化切片 1–7、TASK-029～032 和文档治理检查点；规定任务开始、检查点完成和门禁通过时实时更新。
- 更新根 README、CLAUDE、规格索引和全部受影响的相对链接。

### 验证结果

- 旧文档路径搜索为零；27 个根/`docs/` Markdown 文件的本地链接全部解析成功。
- TASK-000～TASK-032 连续存在，状态与实时任务列表一致。
- `git diff --check` 通过。

### 已知边界

- 文档治理不改变产品行为、架构或版本号。
- TASK-029 仍在进行；完成 TASK-030～032 后再更新切片 5 的完成记录。

本次属于文档治理与任务固化，不分配产品版本号。

## 2026-08-16 完成切片 4 平票辩解与候选重投

### 本轮目标

在切片 3 的正常描述与投票闭环上补齐首个里程碑的完整平票分支：部分最高票候选依次辩解，未进入候选的存活玩家只在候选集合内秘密重投；重投唯一最高票时淘汰，再次平票或全员最高票时本轮无人淘汰并轮换进入下一轮。所有新动作继续遵守服务端权威状态机、原子持久化和字段不存在的信息隔离要求。

### 完成内容

- 扩展共享命令与投影：补充 `SubmitDefenseRequest`；`HumanGameView` 只在轮到人类候选时开放 `SubmitDefense`，只在轮到非候选人类重投时开放 `SubmitVote`，且 `legalVoteTargetIds` 仅含当前平票候选。
- 完成平票纯状态机：`submitDefense` 对候选顺序、当前行动者、2–40 字/两句和原词泄露进行统一校验；候选全部辩解后生成 `revote_started` 并进入 `revoting`；平票候选不参与重投，目标必须属于候选；重投唯一最高票复用淘汰与胜负判断，再次平票生成 `round_ended_without_elimination {reason: revote_tie}`，全员最高票不进入辩解而直接生成 `{reason: all_max}`；下一轮统一按存活相对座序轮换起点。
- 扩展 Agent 白名单输入与假模型：`defend` 不提供投票目标，`revote` 的 `legalTargets` 只包含 `tieCandidates`；`FakeAgentPolicy` 确定性生成合法辩解与重投，并增加显式 `tie-then-eliminate` 测试场景，默认仍为正常流程、不联网、不读密钥。
- 扩展服务端编排与 API：新增 `POST /api/games/:gameId/defenses`；`advanceUntilHumanOrStop` 同时处理 `describe/defend/vote/revote`，遇到人类合法行动时暂停，AI 辩解与重投仍以稳定命令 ID、乐观修订号、私有信念记录和同一事务提交推进。
- 扩展 Web：增加辩解输入及共用内容校验、候选限定的秘密重投、重投进度、候选“被怀疑”状态、AI 辩解/重投等待文案，以及 `revote_started` 和 `round_ended_without_elimination` 漫画分镜；移除切片 3 的“下一切片开放”占位提示。
- 修正预览开发配置：Server 预览直接由项目契约中的 Node.js 20.11.1 执行 `tsx`，避免全局 Node 22 与 `better-sqlite3` ABI 不匹配；平票浏览器验收使用独立 gitignored SQLite 文件和显式假模型场景。

### 验证结果

- 全仓默认测试通过 78 项：shared 39、server 22、web 17；共享测试覆盖部分平票、全员最高票、候选辩解顺序、泄词/越权拒绝、重投目标限制、唯一最高票淘汰、再次平票无人淘汰、公开投影字段不存在；服务端测试覆盖辩解 REST、自动推进、人类候选/非候选暂停、重投秘密进度、全员平票和再次平票；Web 测试覆盖辩解输入、候选限定重投和无人出局分镜。
- `pnpm typecheck`、`pnpm lint`、`pnpm build` 与 `git diff --check` 全部通过；校验统一使用 Node.js 20.11.1 与 pnpm 9.15.9。
- 浏览器在桌面与 375×812 移动视口完整实测“描述—普通秘密投票—部分平票—两名 AI 候选依次辩解—人类非候选秘密重投—统一揭票—候选淘汰—进入下一轮”；移动端 `scrollWidth=innerWidth=375`，无横向溢出，当前 Server 与页面控制台均无错误。
- 信息隔离实测：重投阶段候选以外玩家不出现在投票控件；重投完成前安全帧只有 `vote_progressed {playerId}`，不存在 `targetPlayerId`，完成后只产生一个 `votes_revealed`；活动视图与公开帧均不存在 `camp`、另一方词牌、AI 信念、概率或理由字段。

### 已知边界

- 本切片实现平票规则和可控假模型场景，但 `tie-again` 与 `all-tied` 目前主要由共享/服务端自动化测试验收；浏览器代表性场景使用 `tie-then-eliminate`。
- 主动放弃、人类淘汰后的观战选择、终局身份与信念揭晓属于后续切片。
- 词库仍为里程碑子集；角色 PNG 仍未完成 WebP 压缩。
- 下一切片按里程碑计划强化事务故障注入、重复调度、刷新/重启恢复和 SSE 游标补发。

本次形成可识别的开发切片，但尚未形成产品版本，不分配版本号。

## 2026-08-16 完成切片 3 正常描述与秘密投票闭环

### 本轮目标

在切片 2 基础上建立第一条可自动运行的正常玩法闭环：可控假模型基于严格白名单输入生成描述与选票，人类在轮到自己时提交描述或秘密投票，服务端通过纯状态机推进到统一揭票、淘汰和正常终局；平票安全暂停在 `tie_defense`，完整辩解与重投留待切片 4。切片结束后完成单元、集成和浏览器验证。

### 完成内容

- 扩展共享状态与严格 Schema：`RoundState` 增加描述/投票进度、当前阶段选票与平票候选；`GameSnapshot` 增加 `winnerCamp`、`endReason` 与存活状态且仅 `finished` 可持有胜者；新增 `AgentTurnInput`、信念、描述与投票动作 Schema，以及人类描述/投票请求；`HumanGameView` 增加可安全渲染的公开时间线、当前合法投票目标和只含玩家 ID 的投票完成进度。
- 新增 `content-validation.ts`：Unicode/大小写/空格/中英文标点规范化后执行 2–40 字、最多两句与完整原词连续匹配；人类与 AI 共用同一确定性校验。
- 新增正常流程纯状态机：`submitDescription` 校验阶段/行动者/文本并推进，末位描述后进入 `voting`；`submitVote` 拒绝自投/弃票/非法目标，单票仅生成 `vote_cast(post_game)` 与 `vote_progressed(public)`，末票生成唯一 `votes_revealed(public)`；唯一最高票淘汰并判定平民胜或卧底存活至两人胜，未终局轮换起始玩家，平票只 `tie_declared` 并停在 `tie_defense`。
- 新增 `apps/server/src/agents/`：`agent-input-projector` 白名单构造 Agent 输入，不含真实阵营、其他词牌、其他信念或未揭晓选票；`fake-agent-policy` 按 `gameId/round/actionType/actorId` 确定性脚本返回合法信念、描述与选票，不联网、不读密钥并保留输入供负向测试。
- 重构持久化为通用 `commitTransition`：同一 SQLite 事务内校验旧修订号、追加领域事件、写入 Agent 私有动作、生成允许公开的安全帧、更新快照并记录命令结果；AI 动作使用稳定 `actionId` 与唯一约束保证最多提交一次；公开读取只取 `visibility=public`。
- 扩展 `GameService` 与路由：新增 `POST /descriptions`、`POST /votes`、`GET /events?after=` 与 SSE `/stream`；人类命令提交后触发 `advanceUntilHumanOrStop`，以循环依次执行 AI 直到存活人类行动、`tie_defense` 或终局；SSE 仅作已提交安全帧的通知。
- Web 以 `GameScreen` 替换占位：顶栏、四座位五状态素材、纵向 `ComicTimeline`、当前行动卡与操作区；描述输入即时校验，投票只列出其他存活玩家；单票只显示完成进度，`votes_revealed` 才显示目标关系；淘汰与终局用 CSS 印章；分镜默认跟随底部并提供“回到当前”，遵守 `prefers-reduced-motion`；真人与 AI 五状态素材接入准备页与对局页。

### 验证结果

- 单元与集成测试通过：共 64 项（shared 33、server 16、web 15）；覆盖内容校验边界、状态机描述/投票/揭票/淘汰/两种胜负/轮换/平票停止、Agent 概率覆盖与合法目标、Agent 输入负向（哨兵阵营、其他词牌、其他信念、未揭晓选票、完整词组映射均不存在）、事务原子提交与回滚、单票仅进度公开、末票唯一揭晓、重复调度与过期修订保护。
- `pnpm typecheck`、`pnpm lint`、`pnpm build` 与 `git diff --check` 全部通过。
- 浏览器在 `http://127.0.0.1:9001` 桌面与 375×812 移动视口各完整跑通一局正常胜负：创建、翻牌、开始、AI 自动描述、人类描述、秘密投票、统一揭票、淘汰、终局；两种视口均无横向溢出、无控制台错误。
- 信息隔离实测：进行中投票只出现 `vote_progressed {playerId}`，`targetPlayerId` 仅存在于单个 `votes_revealed` 帧；`GET /api/games/:id`、`events?after=` 与 SSE 帧均无 `camp`、其他玩家词牌、AI 信念/理由、`vote_cast`；DOM 无 `牛奶`（其他阵营词）、`豆浆`（他人词牌语境）、`ownWordCard`/`targetPlayerId` 等私有字段（`civilian`/`undercover`/`reason` 仅为终局 CSS 类名）。

### 已知边界

- 平票只进入 `tie_defense` 并安全停止自动推进，完整辩解与重投在切片 4 实现。
- 重复泄词强制退出、异步 AI 复盘、终局身份/信念揭晓与整套视觉润色不属于本切片。
- 词库仍为里程碑子集；待机素材仍为大尺寸 PNG，WebP 压缩留待后续视觉整理。
- 下一切片实现平票辩解与重投分支。

本次形成可识别的开发切片，但尚未形成产品版本，不分配版本号。

## 2026-08-16 完成切片 2 创建、准备与持久化词牌

### 本轮目标

完成“新对局配置—固定阵容与词牌分配—SQLite 持久化—准备页恢复—本地翻牌—显式开始游戏”的第一条真实纵向链路，并在切片结束后完成单元、集成和浏览器验证。

### 完成内容

- 建立 4 组人工审核词库子集，简单与困难各 2 组；共享 Zod 集合校验覆盖稳定 ID、同组词差异、启用状态和难度可用性。
- 新增服务端权威 `GameSnapshot`、动态玩家集合、可注入随机/ID/时钟，以及创建准备对局、开始第一轮和严格 `HumanGameView` 投影纯函数。
- 接入 `better-sqlite3` 与 Drizzle ORM，建立版本化迁移、词库镜像、对局、玩家私有记录、不可变事件、命令幂等和安全流记录。
- 创建与开始命令在同一事务提交快照、事件、私有玩家记录、安全帧和处理结果；支持重复命令幂等、语义冲突、活动局限制和修订冲突。
- 实现创建、查询活动局、查询单局和开始游戏 REST API；所有进行中响应只包含人类自己的词牌，不包含真实阵营、AI 词牌或完整词组映射。
- Web 实现名称、剪影、难度配置，准备页、四座位、三个 AI 待机素材、本地词牌翻面和刷新恢复；Web 端口统一为 `127.0.0.1:9001`。

### 验证结果

- 单元与集成测试通过：7 个测试文件、31 项测试；覆盖四个卧底座位、词库校验、严格公开投影、创建/开始转换、SQLite 重开恢复、命令幂等/冲突、事务回滚、API 和 Web 本地翻牌。
- `pnpm typecheck`、`pnpm lint`、`pnpm build` 和 `git diff --check` 全部通过。
- 浏览器在桌面与移动视口实际验证新对局、困难模式、AI 素材、隐藏/显示词牌、刷新后重新隐藏、持久化恢复和开始第一轮。
- `POST /api/games` 响应已检查：只包含人类词牌与公开配置，未出现 `camp`、AI 词牌、`civilianWord` 或 `undercoverWord`。
- `http://127.0.0.1:9001` 下无控制台、服务端或横向溢出错误；API 继续监听 `127.0.0.1:3001`。

### 已知边界

- 本切片只在第一轮开始后停留当前行动者，不调用假模型或自动推进。
- 词库仍为首个里程碑子集，不是最终 30 组交付。
- 当前直接使用大尺寸 PNG 待机素材，构建产物约 9 MB；WebP 压缩按素材规范在后续视觉整理中完成。
- 下一切片实现共享描述/投票状态机、内容校验、`FakeAgentPolicy`、Agent 输入隔离、自动推进和漫画分镜。

本次形成可识别的开发切片，但尚未形成产品版本，不分配版本号。

## 2026-08-16 完成切片 1 工程基线与共享契约

### 本轮目标

建立首个里程碑所需的 pnpm 工作区、最小 Web/Server 和共享领域契约，并在切片结束后完成单元测试与实际运行验证。

### 完成内容

- 固定 Node.js 20.11+ 与 pnpm 9.15.9，建立根工作区脚本、TypeScript、ESLint、Vitest、Playwright 和 Browser 预览配置。
- 创建 React/Vite Web、Fastify Server 和不依赖框架或 I/O 的共享 TypeScript/Zod 包。
- 建立首批 `GameStatus`、`GamePhase`、`EndReason`、玩家、命令、事件、`HumanGameView` 和 API 错误 Schema。
- Web 通过 `/api/health` 连接 Fastify，提供与手绘漫画规格一致的最小工程状态页。
- 保持 `pnpm test:live` 为明确未实现且默认失败的显式命令，普通开发与测试不会调用真实模型。

### 验证结果

- 单元测试通过：5 个测试文件、13 项测试，覆盖共享契约、公开视图私有字段剥离、API 错误、Server 健康接口和 Web 连接状态。
- `pnpm typecheck`、`pnpm lint`、`pnpm build` 和 `git diff --check` 全部通过。
- 根目录 `pnpm dev` 同时启动共享包 watch、Fastify `127.0.0.1:3001` 和 Vite `127.0.0.1:9001`，共享包编译无错误。
- 浏览器桌面与 375 × 812 移动视口实际验证通过；健康接口返回 200，页面无控制台或服务端错误。

### 已知边界

- 当前只完成工程基线，没有创建对局、SQLite/Drizzle、完整状态机、SSE 或角色素材接入。
- 下一切片按 `docs/acceptance/milestone-1.md` 实现词库子集、持久化和“创建对局—准备—查看词牌—开始游戏”的第一条纵向链路。

本次形成可识别的开发切片，但尚未形成产品版本，不分配版本号。

## 2026-08-16 检查并索引 AI 角色素材包

- 检查 `素材/` 下 15 张 PNG，确认 DeepSeek、豆包、千问各覆盖待机、思考、发言、被怀疑和被淘汰五种状态。
- 素材视觉方向、角色一致性和状态辨识度可以支持前端开发，模型角色不再依赖几何占位图。
- 建立 `docs/notes/ASSETS.md`，记录文件路径、状态语义、原始尺寸、规范化目录和发布前处理要求。
- 发现 `千文-思考.png` 命名笔误，以及 DeepSeek 思考图画布尺寸与其余文件不同；接入时使用规范化英文文件名并统一导出画布。
- 仍需核验透明通道并补充生成工具、日期、提示词和后期处理记录；人类剪影、词牌、投票标记、场景物件、通用 UI 图形和纸张纹理尚未覆盖。
- 上述缺口不阻塞首个纵向里程碑，可以先由 CSS/SVG 线框实现。

本次属于素材资产登记与工程规格更新，不分配产品版本号。

## 2026-08-16 完成开发就绪评估

- 确认所有参赛角色与复盘 Agent 共用同一个 OpenAI 兼容中转站，只通过模型 ID 区分。
- 复核首版规则、信息隔离、状态机、持久化、前后端通信、测试和首个里程碑范围，当前不存在阻塞开工的产品决策。
- 后续猜词、可选人数、多卧底和模型阵容选择继续保留在路线图中，不进入当前基础玩法开发。
- 项目阶段从规则与架构准备更新为可以进入功能开发。
- 非产品性的工程细节采用合理默认值；关键架构变化、付费调用、提交、推送和发布继续由项目负责人审批。

本次属于开发阶段门禁确认，不分配产品版本号。

## 2026-08-16 确认词库 JSON 事实源

- 首版词库保存在仓库内独立 JSON 文件中，并通过 Zod 校验后同步到 SQLite。
- 每组词使用稳定唯一 ID，包含平民词、卧底词、分类、难度和启用状态。
- 对局创建时保存词组快照，后续修改或停用词组不改变历史复盘。
- 首版不提供词库管理后台，本地 SQLite 不作为 Git 中的内容事实源。

本次属于内容数据架构确认，不分配产品版本号。

## 2026-08-16 确认身份牌自动揭晓

- 终局结果公布后，按座位顺序自动逐张翻开全部身份和词牌，无需玩家点击。
- 翻牌动画只负责展示，完整终局事实在动画前已经持久化。
- 刷新或动画中断不得重复生成终局事件；减少动态效果模式使用无动画或弱动画展示。

本次属于终局交互细化，不分配产品版本号。

## 2026-08-16 确认终局分阶段揭晓

- 终局依次展示结果、全部身份与词牌、确定性事实复盘和异步 AI 总结。
- 主动放弃与系统异常终止只显示对应状态，不生成阵营胜负。
- 身份揭晓后立即开放事实复盘，玩家可以立即开始新对局。
- AI 总结独立异步生成，失败或等待不影响事实记录。

本次属于终局与复盘交互细化，不分配产品版本号。

## 2026-08-16 确认淘汰后的普通观战

- 人类被淘汰并继续观战后仍只查看公开信息，不获得身份上帝视角。
- 其他玩家的阵营、词牌、AI 私有信念和投票理由继续保密。
- 所有私有信息在正常终局后统一揭晓并进入复盘。
- 淘汰观战不改变 SSE、公共状态快照和 Agent 上下文的信息边界。

本次属于观战信息边界细化，不分配产品版本号。

## 2026-08-16 确认开局准备流程

- 对局创建和身份分配完成后先进入准备状态，不立即触发 AI 行动。
- 人类查看词牌并点击“我已记住，开始游戏”后，第一轮才正式开始。
- 翻牌保持为本地显示行为，开始游戏是独立的服务端命令。
- 准备状态下刷新页面不会自动开始，原有身份和词牌分配保持不变。

本次属于开局交互与状态机细化，不分配产品版本号。

## 2026-08-16 确认词组独立随机抽取

- 每局从所选难度的全部已启用词组中独立随机抽取。
- 抽取不参考历史对局，允许连续或短期重复同一词组。
- 首版不实现防连续重复、未使用优先或随机洗牌袋。
- 自动化测试可以注入固定随机种子验证抽取结果。

本次属于开局规则细化，不分配产品版本号。

## 2026-08-16 确认首版词库规模

- 首版内置 30 组词语对，简单和困难各 15 组。
- 每组词启用前人工检查关联度、可描述空间、阵营公平性和泄词风险。
- 首个纵向里程碑可以先用已审核子集验证工程，首版交付时补足全部 30 组。
- 词库结构继续支持后续增加词组、分类和启用状态。

本次属于首版内容范围确认，不分配产品版本号。

## 2026-08-16 确认逐个秘密投票展示

- 规则保持所有玩家独立秘密投票，前端按具体角色依次展示正在投票和已完成投票。
- 全部投票完成前不公开任何目标；最后一票完成后统一揭晓每名玩家投给了谁。
- 选票目标、理由和信念快照在揭晓前保持私有，刷新和 SSE 也不得泄露。
- 该方案增强漫画过程感，但不赋予后投票者任何信息优势。

本次属于投票交互细化，不分配产品版本号。

## 2026-08-16 调整决策讨论方式

- 后续每轮仍只讨论一个决策点，但先以完整评审式说明展开背景和方案差异。
- 方案说明覆盖规则效果、用户体验、实现复杂度、风险、测试和长期影响，并明确推荐理由。
- 默认邀请项目负责人用自然语言自由表达、组合或修改方案，不再频繁使用预设选择题。

本次属于协作方式调整，不分配产品版本号。

## 2026-08-16 统一描述与平票辩解限制

- 平票辩解与普通描述均限制为 2 至 40 字、最多两句。
- 两者共用原词泄露校验，以及相同的长度、句数格式错误与违规处罚边界。
- 服务端不拦截不同玩家、不同轮次或描述与辩解之间的相同文本；避免机械复读只由 AI 提示词约束。
- 描述和辩解仍使用不同动作及事件类型，便于状态机和复盘区分。

本次属于内容规则细化，不分配产品版本号。

## 2026-08-16 明确模型调用失败恢复

- 模型调用进入系统错误后，对当前行动最多自动重试三次，连同初始调用最多尝试四次。
- 从第一次开始重试时，立即向玩家显示脱敏错误类型和重试进度。
- 每次自动重试前固定等待 2 秒，测试中通过可注入时钟避免真实等待。
- 三次重试后仍失败，对局自动标记为“系统异常终止”，不判阵营胜负，也不记为玩家放弃。
- 系统异常终止是不可恢复终局，不能从失败动作继续；仅保留错误摘要和不完整事实复盘。
- 失败尝试不产生公开行动事件；错误分类和时间写入不完整事实复盘。

本次属于错误恢复与终局状态细化，不分配产品版本号。

## 2026-08-16 确认对局自动推进

- 轮到 AI 时由服务端自动编排并推进，轮到存活人类时暂停等待输入。
- 人类被淘汰并选择观战后，剩余 AI 自动连续进行到正常终局。
- 自动行动需要幂等保护，避免刷新、重连或重复调度产生重复事件。
- 前端回看历史时不抢滚动，并保留“回到当前”操作。

本次属于流程与编排确认，不分配产品版本号。

## 2026-08-16 修正主动放弃的里程碑范围

- 主动放弃不是后续功能，而是首个纵向闭环的一部分。
- 人类存活时可以在任意对局阶段二次确认后退出。
- 人类被淘汰后可以选择退出或继续观战。
- 放弃不产生阵营胜负，并保留不完整复盘。

本次属于里程碑范围修正，不分配产品版本号。

## 2026-08-16 明确人类淘汰后的流程

- 人类被淘汰后停止参与所有描述、辩解和投票。
- 玩家可以继续观战，由剩余 AI 自动进行到正常终局。
- 玩家也可以二次确认后放弃并退出，本局不产生阵营胜负且保留不完整复盘。

本次属于流程细化，不分配产品版本号。

## 2026-08-16 明确原词泄露判定

- 服务端对文本进行 Unicode、英文大小写、空格和常见标点规范化后，再执行完整原词连续匹配。
- 空格或标点拆分原词仍会被拦截。
- 同音、拼音、隐喻、拆字和语义近似不作为强制退出处罚依据。
- 人类和 AI 共用同一确定性校验函数。

本次属于规则边界细化，不分配产品版本号。

## 2026-08-16 明确首版阵营分配

- 卧底席位在一名人类和三名 AI 中等概率随机分配。
- 分配结果不直接告知玩家，每名玩家仍只获得自己的词牌。
- 自动化测试可以注入固定随机种子，稳定复现各类身份分配。

本次属于规则细化，不分配产品版本号。

## 2026-08-16 明确全员最高票平票处理

- 所有存活玩家均为最高票候选时，本轮直接无人淘汰并进入下一轮。
- 该情况不执行全员辩解或零人重投，也不随机缩小候选或淘汰玩家。
- 事件日志仍记录全员平票及无人淘汰结果。

本次属于规则边界细化，不分配产品版本号。

## 2026-08-16 明确平票重投参与者

- 最高票平票候选依次重新发言。
- 未进入平票的其他存活玩家听取发言后，针对平票候选重新秘密投票。
- 平票候选不参加重投；参与重投的 AI 在投票前更新私有信念。

本次属于规则细化，不分配产品版本号。

## 2026-08-16 确认首个开发里程碑

- 首个可验收阶段采用纵向最小闭环，而不是领域层或前端单独先行。
- 首先搭建 pnpm workspace、共享类型与状态机、Fastify、SQLite 和 React 骨架。
- 使用可控假模型跑通正常完整对局，并覆盖一次平票辩解与重新投票。
- 首轮同时验证不可变事件持久化和页面刷新后的状态恢复。
- 重复泄露原词的强制退出和异步 AI 复盘留到后续阶段。
- 主动放弃纳入首个里程碑：人类存活时可以随时退出，被淘汰后选择退出或继续观战。
- 真实模型和完整漫画视觉后续分阶段接入。
- 首个里程碑前端采用风格化线框，完成响应式漫画结构、词牌交互和状态反馈，以统一占位角色验证信息层级和布局。
- 最终角色图、精细纹理和完整角色动画不阻塞首个纵向闭环。

本次属于开发顺序确认，不分配产品版本号。

## 2026-08-16 完成项目审计并更新协作治理

### 本轮目标

完整熟悉已有项目内容，梳理长期协作、留档和 Git 治理规则，不进入功能开发。

### 完成内容

- 只读审计全部现有文档和空 Git 仓库状态，确认项目已完成首版需求与总体架构设计，但尚无功能代码、工程配置或首次提交。
- 将需要项目负责人判断的问题规范为一次只确认一个决策点，并提供多个选项、影响和推荐理由。
- 确认重要会话采用完整脱敏聊天稿与结构化项目记录并行的双层留档。
- 确认保留 `master` 单分支直接开发，关键提交、推送、正式发布和重大变更继续由项目负责人审批。
- 修正过期的需求阶段、早期待确认决策、旧工具称谓和命令状态。
- 建立会话归档索引并保存本轮脱敏聊天稿。

### 验证结果

- 首版规则和总体架构的已确认状态在需求、决策和项目记录之间保持一致。
- 本轮只修改文档，没有功能代码、依赖或数据库变化，也没有执行 Git 提交、推送或发布。
- 本次属于项目治理与文档清理，不分配产品版本号。

## 2026-08-16 改为生成项目专用角色图

- 不再直接使用网上主流原图。
- 项目负责人使用统一提示词模板生成三个模型角色。
- 先确认 DeepSeek 默认状态样稿，再派生其他角色和状态。
- 生成工具、日期和提示词需要留档。

本次属于视觉方案修正，不分配产品版本号。

## 2026-08-16 确认优先使用主流原图

- 模型角色优先直接使用网络上的主流 AI 女性形象原图。
- 每张原图必须确定来源和公开仓库再分发权限。
- 权限未明确的搜索图片不进入仓库。

本次属于视觉方向确认，不分配产品版本号。

## 2026-08-16 确认手绘视觉风格

- 使用手绘抖动墨线、平涂色块和轻纸张纹理。
- 不使用复杂厚涂光影。
- 该风格作为所有游戏视觉素材的统一基准。

本次属于视觉确认，不分配产品版本号。

## 2026-08-16 确认角色素材制作流程

- 先完成分镜线框和素材规范。
- 首次只生成一个模型角色默认状态样稿。
- 样稿确认后再批量生成并统一后期处理。

本次属于视觉流程确认，不分配产品版本号。

## 2026-08-15 确认真实模型验收证据

- 真实模型验收生成脱敏 Markdown 报告和关键截图。
- 报告保存到仓库，作为 GitHub 验收证据。
- 敏感配置和无必要的完整模型响应不进入报告。

本次属于验收方案确认，不分配产品版本号。

## 2026-08-15 确认测试分层

- 默认自动化测试使用假模型和临时数据库。
- 单独提供真实模型验收命令。
- 真实调用不进入默认测试和启动流程，避免意外费用。

本次属于验收方案确认，不分配产品版本号。

## 2026-08-15 确认首版访问范围

- 服务仅监听本机地址。
- 首版不实现账户和局域网访问。
- 模型密钥只保存在服务端环境变量中。

本次属于范围确认，不分配产品版本号。

## 2026-08-15 确认实时通信方式

- 玩家命令使用 REST API。
- 思考状态和公开事件使用 SSE 推送。
- 断线后通过事件编号和状态快照恢复。

本次属于架构确认，不分配产品版本号。

## 2026-08-15 确认游戏状态机

- 使用共享包中的自定义强类型状态机。
- 状态转换为纯函数，返回新状态和事件。
- API、模型和数据库层不能绕过状态机修改规则结果。

本次属于架构确认，不分配产品版本号。

## 2026-08-15 确认对局持久化模型

- 使用不可变事件日志记录对局事实。
- 同步维护当前状态快照用于运行和恢复。
- 事件与快照在同一数据库事务中更新。

本次属于架构确认，不分配产品版本号。

## 2026-08-15 确认全栈技术框架

- 前端采用 React、Vite 和 TypeScript。
- 服务端采用 Fastify 和 TypeScript。
- 数据层采用 SQLite 和 Drizzle ORM。
- 使用共享 TypeScript/Zod 包统一类型和校验。
- 使用 pnpm workspace 和单一开发启动命令。

本次属于架构确认，不分配产品版本号。

## 2026-08-14 确认复盘导出

- 每局复盘可导出单个 Markdown 文件。
- 内容覆盖事实时间线、Agent 信念、投票理由和 AI 总结。
- 敏感配置、违规原文和无必要的模型原始响应不进入导出。

本次属于功能确认，不分配产品版本号。

## 2026-08-14 确认应用主要视图

- 默认直接进入新对局配置，不制作宣传首页。
- 顶层视图为新对局、模型档案和历史复盘。
- 未完成对局的恢复入口置于新对局视图。

本次属于交互确认，不分配产品版本号。

## 2026-08-14 确认人类玩家名称

- 名称可以填写，限制 1 至 12 个字符。
- 未填写时默认使用“玩家”。
- 名称只用于当前对局和复盘，不建立用户档案。

本次属于交互确认，不分配产品版本号。

## 2026-08-14 确认人类玩家剪影

- 人类玩家开局时从两个性别剪影中二选一。
- 剪影状态通过姿势、描边、气泡和印章表达。
- 选择只保存在本局配置中。

本次属于视觉确认，不分配产品版本号。

## 2026-08-14 确认模型角色素材

- 三个模型角色使用统一风格的半身手绘人物。
- 每个角色提供四种基础状态。
- 使用统一透明画布并通过分镜构图复用素材。

本次属于视觉确认，不分配产品版本号。

## 2026-08-14 确认漫画流交互

- 新分镜纵向追加并默认跟随当前行动。
- 玩家回看历史时停止强制滚动，提供回到当前操作。
- 分镜各状态保持稳定尺寸，避免布局跳动。

本次属于交互确认，不分配产品版本号。

## 2026-08-14 确认动态漫画界面

- 对局采用纯 2D 手绘动态漫画分镜。
- 游戏行动形成连续分镜，AI 思考使用非信息性草稿动画。
- 复盘复用漫画时间线并展开信念记录。

本次属于交互确认，不分配产品版本号。

## 2026-08-14 确认人类词牌交互

- 词牌默认隐藏，点击翻开，再次点击隐藏。
- 翻牌只影响本地界面，不进入公开事件和 Agent 上下文。
- 使用轻量手绘卡牌翻转效果。

本次属于交互确认，不分配产品版本号。

## 2026-08-14 确认 AI 思考状态

- AI 生成期间展示角色化手绘思考动画。
- 不流式展示模型原始输出和私有信念。
- 所有公开内容通过服务端校验后一次性显示。

本次属于交互确认，不分配产品版本号。

## 2026-08-14 确认投票公开范围

- 对局中公开每名玩家的投票目标。
- 投票理由、身份概率和词语候选保持私有。
- 终局复盘展示每票对应的完整信念快照和理由。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认投票目标限制

- 存活玩家不能自投或弃票。
- 每票必须指向另一名存活玩家。
- Agent 对自己的怀疑仍保留在私有信念和复盘中。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认卧底人数公开

- 卧底人数属于公开对局配置。
- 具体玩家阵营仍由服务端保密。
- Agent 身份概率总量读取动态卧底人数配置。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认 Agent 联合推理方式

- Agent 先猜测多个异阵营词候选。
- 根据发言与候选词的接近程度推导玩家身份概率。
- 玩家概率包含 Agent 自己，总量由本局卧底人数决定。
- 投票同时记录词语依据和身份概率快照。

本次属于方案确认，不分配产品版本号。

## 2026-08-14 确认模型结构化输出

- 模型按提示词返回 JSON，服务端使用 Zod 校验。
- 首次结构错误执行一次格式修复，再失败按系统错误处理。
- 原生 JSON Schema 只作为可选优化，不成为中转站必要能力。

本次属于方案确认，不分配产品版本号。

## 2026-08-14 确认模型接口协议

- 首版统一使用 OpenAI 兼容中转接口。
- 三个模型通过模型 ID 和连接配置区分。
- 共用模型客户端和错误处理，首版不接入厂商原生 SDK。

本次属于方案确认，不分配产品版本号。

## 2026-08-14 建立项目管理基线

### 本轮目标

建立适用于后续全栈开发和多轮迭代的中文记录体系。

### 完成内容

- 建立协作规范。
- 建立核心命令、需求、关键决策和开发记录。
- 建立较大版本变更记录。
- 明确关键把关点、版本号使用范围和中文书写规范。

### 验证结果

- 文档之间的职责边界明确，并通过项目首页统一索引。
- 本次变更属于管理基线建设，不分配产品版本号。

## 2026-08-14 梳理产品需求与框架选型

### 本轮目标

将项目背景转化为可验收要求，并分析首版技术框架与 Agent 编排方式。

### 完成内容

- 记录完整产品目标、核心验收要求、评价重点和最终交付物。
- 提出以 Next.js 全栈应用、SQLite 和服务端权威状态机为核心的首版方案。
- 明确采用隔离上下文的受控 Agent 编排，不采用自由群聊式 Agent 架构。

### 当前结论

- 该方案已于 2026-08-15 通过 DEC-048 确认，早期 Next.js 方案由 React + Vite 前端与 Fastify 服务端架构替代。
- 本次为分析与文档细化，不分配产品版本号。

## 2026-08-14 扩展 Agent 认知与视觉方向

### 本轮目标

定义 AI 的猜测过程、复盘数据和 Multi-Agent 边界，并明确轻量 2D 手绘风格的素材策略。

### 完成内容

- 将 AI 玩家从无状态调用扩展为具有私有信念状态的独立 Agent。
- 定义自己阵营、双方词语、其他玩家身份和理由等复盘字段。
- 提出独立玩家 Agent 加赛后分析 Agent 的分层方案。
- 提出项目内最小素材库与 AI 角色生图相结合的视觉方案。

### 后续确认结果

- 玩家开局不知道自己的真实阵营，已由 DEC-007 确认。
- 使用独立的赛后复盘分析 Agent，已由 DEC-029 确认。
- 采用项目专用素材规范和 AI 生图补充角色，已由 DEC-055、DEC-056 和 DEC-058 确认。

本次为方案深化，不分配产品版本号。

## 2026-08-14 修正 Agent 的词语猜测模型

### 修正内容

- 移除同时猜测平民词和卧底词的设计。
- AI 只对自己的阵营作出判断。
- AI 将另一个词统一视为敌对阵营身份词，并可维护多个候选、置信度和依据。
- 对局结束后再依据揭晓的真实阵营解释候选词的阵营归属。

本次属于方案修正，不分配产品版本号。

## 2026-08-14 确认身份信息规则

- 确认所有玩家只知道自己的词牌，不知道自己的真实阵营。
- 确认淘汰只公开玩家名单，不公开阵营和词牌。
- 统一使用“词牌”指代玩家看到的牌，避免与真实阵营身份混淆。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认未完成对局恢复

- 对局按已完成事件自动保存。
- 重新进入时可继续未完成对局。
- 玩家也可放弃旧对局并开始新局，旧局保留不完整复盘。

本次属于方案确认，不分配产品版本号。

## 2026-08-14 确认模型任务调度

- 实时对局模型调用优先于后台复盘。
- 已发出的复盘请求不取消，尚未开始的任务延后。
- 后台最多同时执行一个复盘任务。

本次属于方案确认，不分配产品版本号。

## 2026-08-14 确认异步复盘

- 终局后自动创建持久化复盘任务。
- AI 复盘在后台异步生成，不阻塞新对局。
- 服务重启后可恢复任务，失败时保留事实复盘并允许重试。

本次属于方案确认，不分配产品版本号。

## 2026-08-14 确认复盘模型配置

- 复盘 Agent 使用独立模型连接和调用上下文。
- 可复用参赛模型服务，也可使用第四个模型。
- 复盘模型可单独替换，记录中只保留非敏感模型标识。

本次属于方案确认，不分配产品版本号。

## 2026-08-14 确认赛后复盘架构

- 程序生成不可被模型修改的事实时间线。
- 独立复盘 Agent 基于完整终局记录生成全局分析。
- AI 总结与事实记录分开展示，生成失败不影响基础复盘。

本次属于方案确认，不分配产品版本号。

## 2026-08-14 确认主动放弃规则

- 人类玩家可在二次确认后放弃对局。
- 放弃不产生阵营胜负，也不视为玩家被淘汰。
- 已产生的事件和信念保留为不完整复盘。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认不限制回合数

- 正式对局持续到满足阵营胜负条件。
- 不设置轮数平局或随机强制淘汰。
- 自动化测试使用独立执行保护，不改变游戏规则。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认第一轮投票

- 第一轮全员描述完成后立即秘密投票。
- 不设置免投票观察轮。
- 第一轮信念和投票进入复盘时间线。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认难度选择

- 人类玩家在开局前选择简单或困难。
- 服务端只从所选难度的启用词语中抽取。
- 难度写入对局配置和复盘记录。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认词语阵营与难度

- 词语对固定区分平民词和卧底词，不随机交换。
- 词库提供简单和困难两种难度。
- 简单词更容易形成合规描述，困难词更考验措辞和推理。
- 运行时 Agent 不接触完整词库和阵营映射。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认模型角色化方向

- 使用不同厂商或不同模型作为 AI Agent 的角色身份。
- 每个模型角色只预设简短性格与基础提示词。
- 角色展示、模型连接和游戏规则相互解耦。
- 中转接口与密钥不进入仓库和复盘。

本次属于方案确认，不分配产品版本号。

## 2026-08-14 确认首版词语来源

- 使用项目内置、人工筛选的词语对。
- 词语对记录分类、难度和启用状态。
- 首版不使用模型动态生成和人类自定义词语。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认模型轻人格方案

- 三个模型共享统一的核心游戏提示词。
- 每个模型只附加简短的表达风格提示。
- 人格提示不得改变信息权限、推理结构和游戏规则。

本次属于方案确认，不分配产品版本号。

## 2026-08-14 确认首版模型阵容

- 首版固定使用 DeepSeek、豆包、千问三个模型身份。
- 首版不开发阵容和人数选择界面。
- 二期支持选择角色数量，并为每个 AI 座位选择模型。
- 核心引擎继续保持模型身份和人数可配置。

本次属于方案确认，不分配产品版本号。

## 2026-08-14 确认提示词公开范围

- 身份卡只展示模型角色的人格提示词。
- 系统控制提示词与人格提示词分层维护。
- 系统提示词模板在代码和开发文档中可审查，运行时私密信息不得落盘。

本次属于方案确认，不分配产品版本号。

## 2026-08-14 确认模型角色展示

- 对局中直接使用真实模型简称作为 AI 玩家名。
- 对局界面不增加模型标签。
- 对局外通过身份卡展示三个性格标签和提示词。
- 人格提示词与系统控制提示词分层维护。

本次属于方案确认，不分配产品版本号。

## 2026-08-14 确认首版玩家人数

- 首版固定为一名人类和三名 AI。
- 首版固定一名卧底和三名平民。
- 只在创建配置层限制人数，核心状态和规则按玩家集合设计。
- 人数选择和多卧底玩法留待后续较大迭代。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认 Agent 信念更新时机

- AI 只在需要执行公开行动时更新信念。
- 信念更新与描述、辩解或投票尽量合并调用。
- 每个行动保存当时的信念快照，后续更新不得覆盖。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认错误分类

- 只有直接泄露自己的词牌原词属于游戏违规。
- 描述长度和句数问题归类为格式错误；相同公开文本不做服务端拦截。
- 模型接口和结构化输出异常归类为系统错误。
- 格式错误与系统错误均不处罚玩家或改变胜负。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认原词泄露处罚

- AI 和人类玩家共用服务端原词校验。
- 首次泄露原词时允许重新发言一次。
- 再次泄露时公开违规结论、强制退出并立即进行胜负判断。
- 原始违规内容不进入其他 Agent 的公开上下文。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认描述限制

- 描述限制为 2 至 40 个字符且最多两句。
- 禁止包含自己的词牌原词；不再以与历史公开文本相同作为服务端格式错误。
- 使用提示词自检与服务端确定性校验形成双重防线。
- 违规的 AI 描述作废并重新生成，不进入公共信息。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认平票处理

- 平票候选先进行简短辩解，再对候选人重新秘密投票。
- 辩解触发存活 AI 的信念更新。
- 再次平票时，本轮无人淘汰。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认投票方式

- 采用独立秘密投票，全部完成后统一公开。
- AI 投票时冻结信念分布和投票理由。
- 复盘使用历史快照，不使用赛后状态反推投票依据。

本次属于规则确认，不分配产品版本号。

## 2026-08-14 确认胜负规则开发顺序

- 基础玩法采用经典人数胜负判定。
- 基础玩法完成并验收后，再开发全阵营猜词玩法。
- 后续猜词玩法同时支持卧底和平民猜测敌对阵营词。

当前仅记录开发路线，不分配产品版本号。

## 2026-08-14 确认发言顺序

- 首轮随机决定顺序。
- 后续每轮轮换起始玩家并保持座位相对顺序。
- 淘汰玩家不参与后续发言。

本次属于规则确认，不分配产品版本号。
