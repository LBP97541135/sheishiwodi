# 2026-08-16：项目完整审计与协作治理

- 日期：2026-08-16（具体时间不可用）
- 参与者：项目负责人、AI 开发助手
- 主题：完整查看已有项目内容，确认协作、留档与 Git 分支治理
- 脱敏说明：本稿只保留参与者可见的项目对话与交互选择，不包含系统提示、工具内部消息、内部路径、密钥或敏感接口地址。

## 完整对话

### 项目负责人

现在先来完整的看一下这个项目的已有内容，熟悉一下项目。

现在我们先来梳理一下规则：

1. 我需要你去做一次全栈开发。
2. 开发中会有多次的版本迭代。
3. 关键分支必须由我来把关。
4. 我希望你可以想办法导出和保留我们的聊天和开发记录，便于后续的复盘的分析。
5. 遇到问题的时候我们一个一个确认，每次提出一个点，并给我多种选择，然后我们来进行讨论。

### AI 开发助手

我会先按你定的方式推进：一次只确认一个关键点，并提供多种选择。先确定这轮“熟悉项目”的检查深度；在你确认前，我不会修改任何项目文件。

请选择检查深度：

- 完整只读审计（推荐）：系统检查目录、代码、配置、依赖、数据、构建与测试，最后给出项目全景、问题清单和待确认事项；不修改项目。
- 结构快速浏览：只看目录、核心文件和主要流程，先快速建立整体认知。
- 边检查边验证：除完整审计外，还实际安装依赖并运行构建、测试或启动命令；可能生成缓存和依赖目录。

### 项目负责人

选择：完整只读审计。

### AI 开发助手

我已经开始只读审计。你刚才确认的协作规则会作为长期约束单独记录；项目本身这轮不会被修改，也不会安装依赖或运行可能写入文件的构建命令。

### AI 开发助手

只读审计已经完成。现状很明确：项目已经形成了非常详细的产品、规则、架构和协作基线，但还没有进入功能代码阶段。下一步先按你的要求只讨论一个尚未真正落地的关键点：聊天原文如何与现有结构化开发记录共同保存。

请选择聊天与开发过程的长期留档方式：

- 双层留档（推荐）：每次重要会话保存一份脱敏的完整聊天稿，同时继续维护命令、需求、决策、开发和版本记录；复盘信息最完整，但仓库内容会更多。
- 仅结构化记录：保持现有方式，只保存核心命令、有效需求、关键决策和开发结果，不保存聊天原文；最简洁，但会丢失讨论过程。
- 原文仓外保存：仓库只存结构化记录和会话索引，完整聊天稿单独导出到仓库外的归档目录；隐私与仓库整洁度更好，但迁移时需同时保管两处。

### 项目负责人

选择：双层留档。

### AI 开发助手

完整只读审计的结论是：这个项目目前是“需求与架构设计仓库”，还不是代码仓库。现有内容只有 7 个 Markdown 文件，没有源码、依赖清单、测试、环境变量样例、构建配置、`.gitignore` 或许可证；Git 仓库也尚无首次提交、远程地址和实际分支历史，`HEAD` 只是指向尚未创建提交的 `master`。

产品基线已经相当成熟。现有文档记录了 57 条核心命令和 58 项架构/产品决策，首版人数、游戏流程、信息隔离、Agent 私有信念、模型阵容、错误恢复、复盘导出、前后端技术栈、状态机、事件日志、REST/SSE、异步任务和测试分层均已明确。按当前方案，预期主链路是：React 前端通过 REST 提交命令，Fastify 调用共享的纯状态机，在一个 SQLite 事务内追加事件并更新快照，再通过 SSE 只推送已确认的公开事件；模型编排层为每个 Agent 构造隔离上下文，Zod 校验输出，私有信念仅在终局复盘中开放；异步复盘 Worker 独立读取终局事实和私有历史。

当前有少量文档状态没有同步，但不阻塞开工：`DEC-004`、`DEC-005`、`DEC-006` 仍标为“待确认”，实际上已经被后续决策覆盖；需求文档仍笼统写着“其余规则待确认”；`CMD-003` 仍显示“分析中”；协作规范还使用了旧的“Codex 负责”表述。这些适合在正式搭建工程骨架时统一清理。

你刚才已经确认采用“双层留档”：重要会话保存完整脱敏聊天稿，同时维护命令、需求、决策、开发和版本等结构化记录。我已经将它记为长期协作约束；由于本轮是只读审计，尚未改动项目文件。

接下来只确认一个开工前的关键治理点：分支策略。

参考来源：

- [项目说明](../../../README.md)
- [协作规范](../../notes/COLLABORATION.md)
- [需求记录](../../acceptance/REQUIREMENTS.md)
- [关键决策记录](../../notes/DECISIONS.md)
- [核心命令记录](../COMMAND_LOG.md)
- [开发记录](../PROJECT_LOG.md)
- [版本变更记录](../../../CHANGELOG.md)

请选择 Git 分支治理方式：

- `main` + 短分支（推荐）：默认分支改为 `main`；每项工作使用短期 `feature/*` 或 `fix/*` 分支；完成测试和变更说明后，由项目负责人确认才能合并到 `main`。较大版本再打标签。
- 完整 Git Flow：使用 `main`、`develop`、`feature/*`、`release/*` 和 `hotfix/*`；阶段隔离更严格，但对当前单人首版开发偏重。
- 单分支开发：保留 `master` 并直接开发，只在关键提交和发布前由项目负责人审批；最简单，但代码审查、回滚和并行迭代能力较弱。

### 项目负责人

选择：单分支开发。

### AI 开发助手

已确认：项目保留 `master` 单分支直接开发，不创建 `feature/*`、`develop` 或 `release/*` 分支。作为配套约束，我不会自行提交、推送或发布；关键提交、重大架构变更、破坏性操作和发布节点仍由你审批。

下一步如何把本轮确认的协作规则落实到项目中：

- 更新文档不提交（推荐）：下一步先统一修正文档中的过期状态，并补充双层留档、单分支治理和逐项确认规则；修改后给项目负责人审阅，不提交 Git。
- 继续讨论规则：先不改项目文件，继续逐项讨论剩余规则，全部确认后再统一落盘。
- 准备首次提交：更新规则文档并整理首次提交内容，但实际执行 Git 提交前仍单独向项目负责人确认。

### 项目负责人

选择：更新文档不提交。

### AI 开发助手

收到。按已确认方案更新规则文档、修正过期状态并建立本轮聊天归档；本轮不执行 Git 提交。

## 本轮形成的有效结论

本节是对对话的索引性概括，不替代结构化项目记录。

- 项目已完成首版产品规则和总体架构确认，可进入工程基线搭建准备阶段。
- 需要项目负责人判断的问题一次只提出一个，并给出多个选项、影响和推荐理由。
- 重要会话采用完整脱敏聊天稿与结构化项目记录并行的双层留档。
- Git 保留 `master` 单分支开发，但关键提交、推送、正式发布、破坏性变更和重大架构调整仍需项目负责人确认。
- 本轮只更新文档，不执行 Git 提交。
